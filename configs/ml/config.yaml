## Config Hydra : assemble les groupes model / data / train

defaults : 
  - model : naive_baseline
  - data : splid
  - train : default 
  - __self__ 

seed : 42

hydra : 
  run :
   dir : ${hydra:runtime.cwd}/outputs/ml/${now:%Y-%m-%d_%H-%M-%S}

  
